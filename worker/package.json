{
  "name": "silentcine-worker",
  "version": "1.0.0",
  "description": "SilentCine audio extraction worker — runs locally with ffmpeg",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.96.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
